{
	"name": "obsidian-image-resizer",
	"version": "1.0.0",
	"description": "Resize all embedded images in the current document to a configurable fixed width.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"test": "node --test"
	},
	"keywords": [
		"obsidian",
		"plugin"
	],
	"author": "akali93",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.11.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "0.20.0",
		"obsidian": "latest",
		"typescript": "5.4.5"
	}
}
